# Unreleased (TW#26717377)

## Added
- `v6` prop: opt-in IIA v6 reference section card (CRM Contact v1.4 / Company v1.0 refSections), ported from module-v6-crm ReferenceSection.vue. Inline prototype styles; relies on `.chip`, `.crm-refwrap` and `ins-fade-in-up` from the insites.css record-page layer.
- `eager` prop, `mounted` state (wrapper class `mounted`, body `data-mounted`), `insMount` event, `isMounted()` method.
- `isOpen()` method.
- `count` accepts a string as well as a number ("5 profiles"); empty string hides the badge.

## Unchanged
- Default rendering, class names, `insToggle`, `didLoad`, `openPanel()`, `closePanel()`, `toggle()`.

# v2.0.0

## Changes from V1
- None
