# Unreleased
- [TW#26371963](https://pm.cbo.me/#tasks/26371963) New component: the IIA v6 shell's dismissible help panel from the Admin Shell v1.5 design. Card with the design's animated dashboard graphic, a heading, a body (prop or slot) and a "Dismiss permanently" control. Dismissal is stored per administrator in the administrator-preferences store (key `help_panels:dismissed`, a JSON list of panel keys) so it holds across devices; `insHelpDismiss` bubbles to the document for the header's toast and restore row, and the panel shows again when `ins-header` emits `insHelpRestore` after clearing the list. Props `panelKey`, `heading`, `body`, `graphic`, `preferencesEndpoint`, `dismissedMessage`.
