# Unreleased
- [TW#26371963](https://pm.cbo.me/#tasks/26371963) `updateCrumbs()` now also emits `insBreadcrumbsChange` (bubbles to the document) with the trail it was given, so the v6 shell header can draw the page's real trail in its breadcrumb bar. Additive: rendering, `routePage` and the `ins_breadcrumbs` localStorage write are unchanged.

# v2.0.0

## Changes from V1
- None