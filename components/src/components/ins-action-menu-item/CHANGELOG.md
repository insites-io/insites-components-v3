# Unreleased
- [TW#26717377](https://pm.cbo.me/#tasks/26717377) Add `divider` (1px separator row, skipped by arrow keys) and `slot-label` (right-aligned muted secondary text, used by the design's channel flyouts). Record-page row and flyout styles apply only under `ins-action-menu[variant="record"]`. Nested items added after mount are now detected. Default rendering is unchanged.
