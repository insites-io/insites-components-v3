# Unreleased
- [TW#26673778](https://pm.cbo.me/#tasks/26673778) Remove `hover-submenus`, added the previous day and never adopted. Hover-to-open is an accessibility failure per the CRM Company v1.0 handover, and hover alongside click actively breaks clicking: entering the row opens the flyout, so the click that follows toggles it shut. Submenus open on click or ArrowRight only. Removing a published prop deviates from the additive-only rule; done here because the prop was one day old, had no consumer in any module, and enabled a defect rather than a behaviour.
- [TW#26717377](https://pm.cbo.me/#tasks/26717377) Add `variant="record"` (IIA v6 record-page kebab: 36px `--ins-main` bordered trigger, 240px card panel, left flyouts) and `trigger-tip` (`data-tip` on the trigger). Both opt-in; the default rendering is unchanged.
