# Unreleased
- [TW#26717377](https://pm.cbo.me/#tasks/26717377) Add `variant="record"` (IIA v6 record-page kebab: 36px `--ins-main` bordered trigger, 240px card panel, left flyouts), `hover-submenus` (flyouts open on pointer hover) and `trigger-tip` (`data-tip` on the trigger). All opt-in; the default rendering is unchanged.
