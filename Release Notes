Release Notes

*Insites Components* `v3.1.0` — 2026-09-10 (admin CDN line, `v3/latest`; pinned at `v3/3.1.0`)

First pinned release of the v6 admin shell line. `v3/3.0.0` (4 Sep) was the byte-identical fork of insites-components-v2 at v2.15.6 plus the seven CRM record-page components; everything since went to `v3/latest` only. This pin captures what is live.

*Since 3.0.0*
- `ins-header`, `ins-sidebar`, `ins-sidebar-item`: `variant="v6"`, the IIA Admin Shell v1.5 design — rail groups and flyouts, 56px header, instance switcher UI with production warning, help and account menus, breadcrumb bar, tooltips, dark mode, keyboard shortcuts (TW#26371963)
- `ins-header`: breadcrumb bar draws the page's own trail (`insBreadcrumbsChange` from `ins-breadcrumbs`); toast; per-administrator "Don't show me again" on the production warning; "Show help panels" restore on the preference store; `lockScreen()` method replaces the lock row
- `ins-help-panel`: new — the design's dismissible help card with per-administrator persistence
- `ins-breadcrumbs`: emits `insBreadcrumbsChange` on `updateCrumbs()`
- `ins-action-menu`: hover-submenus removed (TW#26717377)
- Shell stylesheet: v6 tokens and chrome layer; hides module page-title crumbs under the shell; the content column is the scroller

*Rollback*: `scripts/rollback.sh 3.0.0` (drops the shell — only if the shell itself is the fault).

---

Hi @here *Insites Components* `v2.0.0` is now released.

Please see the latest documentation for the updates
http://components.insites.io/

*Change Log*
`Breaking change`
- StencilJS version updated from `v0.18.0` to `v1.14.0`;
- Styles are now separated from the components for theming and caching
- Updated event names to make it predictable and to avoid native DOM event conflicts
    - example: from `onClickInsButton` to `insClick`

- Methods are now asynchronous this is to avoid race condition when the method is called during component render
- `ins-base-table` is now updated to `ins-table`

*Improvements*
- Insites Component Colors is now using CSS custom properties which you can customise for your project's theme
  - Demo: https://cbo.d.pr/xxvncz

- Third Party libraries are now built-in the components
- Caching: Assets now has Cache-Control with max-age of 1 year
- Compression: Assets are now being served as gzipped
- Versioning:
  - Projects will now have an option to pick which version to use
  - Projects Will no longer be affected by Continuous Deployment from v1 that causes `403` issues

Please check here on how to migrate from V1
http://components.insites.io/#/app/migrating-from-v1

V1 docs is available here - http://components.insites.io/v1/
Insites Components V1 will be supported until `December 31, 2020`